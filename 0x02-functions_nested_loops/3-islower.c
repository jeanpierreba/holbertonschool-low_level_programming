#include "holberton.h"

/**
 * _islower - check for lowercase character
 * @c: lowercase alphabet variable
 * Return: 0 if lowercase character 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
